{
  "name": "nfticket-subgraph",
  "version": "0.1.0",
  "description": "Subgraph for indexing NFTicket Protocol events",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nfticket-protocol",
    "create-local": "graph create --node http://localhost:8020/ nfticket-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ nfticket-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nfticket-protocol"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.67.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
